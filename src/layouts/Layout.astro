---
import '@fontsource-variable/montserrat';
import '../assets/script.ts';
import '../assets/style.css';
import Welcome from '../partials/Welcome.astro';

interface Props {
	title: string;
}

const { title, ...others } = Astro.props;
---

<!doctype html>
<html lang="en-US">
	<head>
		<title>{title} – JAVA WiFi</title>
		<meta http-equiv="Content-Type" content="text/html; charset=UTF-8" />
		<meta http-equiv="pragma" content="no-cache" />
		<meta http-equiv="expires" content="-1" />
		<meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
		<link rel="icon" href="/favicon.ico" />

		<slot name="head" />
	</head>

	<body
		class="flex min-h-[90vh] w-full flex-col items-center justify-center bg-[#f5f5f5] text-sm font-light leading-normal text-brand-black sm:text-base"
	>
		<div class="my-4 w-[90%] max-w-[30em] border border-gray-200 bg-white p-10" {...others}>
			<Welcome />

			<slot />
		</div>
	</body>
</html>
